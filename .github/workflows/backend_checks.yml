name: Ruby

on: [push]

jobs:
  rspec:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.2.2'
        bundler-cache: true

    - name: Install dependencies
      run: |
        sudo apt install -y sqlite3
        gem install bundler
        bundle install --jobs 4 --retry 3

    - name: ver conteudo da variavel
      run: |
        echo $RACK_ENV

    - name: Set up database
      env:
        RACK_ENV: test
      run: |
        echo $RACK_ENV
        bundle exec rake db:create db:migrate
  
    - name: Run tests
      env:
        RACK_ENV: test
      run: |
        echo $RACK_ENV
        bundle exec rake spec:all